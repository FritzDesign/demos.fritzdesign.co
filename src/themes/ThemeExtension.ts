import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  components: {},
  colors: {
    Brand: {},
    UI: {}
  },
  fonts: {
    Heading: 'Marcellus',
    Body: 'Poppins',
    Alt: 'Inter'
  }
});

export default theme;
